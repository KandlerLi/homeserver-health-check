name: Checks

on:
  pull_request:
  workflow_dispatch:

jobs:
  terraform:
    uses: KandlerLi/gha-common/.github/workflows/terraform-checks.yml@v0.0.7
    with:
      aws_region: us-east-1
      extra_repo_vars: "ALERT_EMAIL"
    secrets: inherit
    permissions:
      contents: read
      id-token: write
      packages: write
