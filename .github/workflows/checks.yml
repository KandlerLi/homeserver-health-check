name: Checks

on:
  pull_request:
  workflow_dispatch:

jobs:
  terraform:
    uses: KandlerLi/gha-common/.github/workflows/terraform-checks.yml@v0.0.14
    with:
      aws_region: us-east-1
    secrets: inherit
    permissions:
      contents: read
      id-token: write
      packages: write
      pull-requests: write
